#!/bin/sh

# Make VuCI and rpcd forget descriptors that opkg has just removed.
[ -n "${IPKG_INSTROOT:-}" ] && exit 0
/etc/init.d/rpcd restart >/dev/null 2>&1 || true
/etc/init.d/uhttpd restart >/dev/null 2>&1 || true
exit 0
