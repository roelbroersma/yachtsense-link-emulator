name: Apply tested v1 relay update

# Temporary bootstrap-branch workflow retained only to carry the source bundle.
# The authoritative publisher now lives temporarily on main.
# retrigger default-branch PR validator after workflow-permission fix
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Source bundle carrier branch; publishing is handled from main.'
